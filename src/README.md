# MindEase Aurora Vue

MindEase Aurora 是一套 Vue3 前端应用，用于情绪记录、心理测评、AI 咨询和预约管理。

## 启动

```bash
npm install
npm run dev
```

开发环境默认连接 `http://localhost:8080`，生产环境默认使用 `/api`。

## 技术与风格

- Vue3 + Vite + TypeScript
- Vue Router + Pinia + Axios
- 自建沉浸式深色视觉系统
- API 层集中在 `src/api`
