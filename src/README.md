# MindEase Clinic React

MindEase Clinic 是一套 React 前端应用，用于情绪记录、心理测评、AI 咨询和预约管理。

## 启动

```bash
npm install
npm run dev
```

开发环境默认连接 `http://localhost:8080`，生产环境默认使用 `/api`。

## 技术与风格

- React + Vite + TypeScript
- React Router + Context 登录态 + Axios
- 专业临床工作台风格
- API 层集中在 `src/api`
